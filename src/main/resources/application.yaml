spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/school_lang_db
    username: postgres
    password: papamama1R
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
#  main:
#    allow-circular-references: true

jwt:
  secret: huyf7sa87fda7fga7gfdgfguaf2hf8heuihfagoih34
  lifetime: 30m
